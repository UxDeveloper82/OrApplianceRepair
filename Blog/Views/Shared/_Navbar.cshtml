<nav class="navbar navbar-expand-md navbar-dark bg-primary flex-column">
    <div class="container">
        <!-- Branding Image -->
        @Html.ActionLink("OR-SERVICES", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarsExampleDefault" aria-controls="navbarsExampleDefault" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarsExampleDefault">
            <!-- Left Side Of Navbar -->
            <ul class="navbar-nav mr-auto">
                <li class="nav-item">@Html.ActionLink("About", "About", "Home", new { area = "" }, new { @class = "nav-link" })</li>
                <li class="nav-item">@Html.ActionLink("My-Blog", "Index", "Post", new { area = "" }, new { @class = "nav-link" })</li>
                <li class="nav-item">@Html.ActionLink("Send Message", "New", "MyMessages", new { area = "" }, new { @class = "nav-link" })</li>
            </ul>
            <ul class="nav navbar-nav navbar-right">
                @if (User.Identity.IsAuthenticated)
                {
                    <li class="nav-item"><a class="nav-link" asp-controller="Panel" asp-action="Index">Admin Panel</a></li>
                    <li class="nav-item"><a class="nav-link" asp-controller="Auth" asp-action="Logout">Logout</a></li>
                }
                else
                {
                    <li class="nav-item">@Html.ActionLink("Sign In", "Login", "Auth", new { area = "" }, new { @class = "nav-link" })</li>
                }
            </ul>
        </div>
    </div>
</nav>